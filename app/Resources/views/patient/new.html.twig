{% extends 'base.html.twig' %}

{% block body %}
<div class=" m-4">
    <h1>Patient creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-primary" type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
    <br>
       
            <a class="btn btn-outline-primary btn-lg" href="{{ path('patient_index') }}">Back to the list</a>
        
    </ul>
{% endblock %}
