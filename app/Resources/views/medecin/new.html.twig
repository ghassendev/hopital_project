{% extends 'base.html.twig' %}

{% block body %}
<div class=" m-4">
    <h1>Medecin creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input  class="btn btn-primary" type="submit" value="Create" />
    {{ form_end(form) }}
<br>
    <ul>
        
            <a class="btn btn-outline-primary btn-lg" href="{{ path('medecin_index') }}">Back to the list</a>
        
    </ul>
    </div>
{% endblock %}
